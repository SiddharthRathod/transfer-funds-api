framework:
    lock:
        # Default lock store (used by autowired LockFactory)
        default: '%env(LOCK_DSN)%'

        # Named store specifically for fund transfers
        transfer: '%env(LOCK_DSN)%'
